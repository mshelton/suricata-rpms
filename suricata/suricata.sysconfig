# The following parameters are the most commonly needed to configure
# suricata. A full list can be seen by running /sbin/suricata --help
# -i <network interface device>
# --user <acct name>
# --group <group name>

# What interface should Suricata listen on?
INTERFACE=eth0

# What permissions should Suricata user run as?
#  This user and group should have very few privileges.
USER=suricata
GROUP=suricata

# Add options to be passed to the daemon
OPTIONS=""
